use ic_cdk::storage;
mod types;
use std::rc::*;

use types::VideoStreamingCompany;

impl VideoStreamingCompany {
    fn get_user_count(){

    }
}



#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
